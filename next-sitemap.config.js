/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: "ajibest.vercel.app", // replace with your website
  generateRobotsTxt: true, // optionally generate robots.txt
  changefreq: "daily",
  priority: 0.7,
  sitemapSize: 5000, // max URLs per sitemap
};
